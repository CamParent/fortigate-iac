name: FortiGate IaC Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  terraform:
    name: Terraform Plan & Apply
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Create tfvars from secrets
        run: |
          cat > terraform.tfvars << EOF
          fortios_token        = "${{ secrets.FORTIOS_TOKEN }}"
          fortios_branch_token = "${{ secrets.FORTIOS_BRANCH_TOKEN }}"
          EOF

      - name: Debug - Test FortiGate API
        run: |
          curl -k -H "Authorization: Bearer ${{ secrets.FORTIOS_TOKEN }}" \
          https://192.168.1.112/api/v2/cmdb/system/global | head -c 100

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format Check
        run: terraform fmt -check

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve